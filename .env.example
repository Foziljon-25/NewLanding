# Backend base URL. The frontend app appends /calculator automatically
# unless this value already ends with /calculator.
NEXT_PUBLIC_CALCULATOR_API_BASE_URL=http://localhost:3001

# Landing request form endpoint. The frontend app appends /requests
# unless this value already ends with /requests.
NEXT_PUBLIC_PROCARE_REQUEST_API_URL=http://localhost:3001
